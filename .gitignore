# IDEs and editors
.idea
*.iml
*.iws
.project
.classpath
*.launch
.settings
*.sublime-workspace
.vscode

# Dependency directories
node_modules

# TypeScript cache
*.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*

# Packaged SuiteCloud projects
build

# misc
.sass-cache

# Optional npm cache directory
.npm

# System Files
.DS_Store
Thumbs.db

# Output of 'npm pack'
*.tgz

# Project config
project.json
.compound-engineering/*.local.yaml

# Secrets
.env
private-key.pem
certificate.pem

# Local agent workflow + internal docs (not for public GitHub view)
.claude/
.compound-engineering/
AGENTS.md
ARCHITECTURE.md
BUILD_PLAN.md
CHANGELOG.md
CLAUDE.md
docs/
tasks/

# Unit tests + mocks tracked so CI can run them
# (Previously gitignored — caused CI Jest step to find 0 tests; see fix on 2026-05-28)

# Pre-JSX legacy archive — kept on disk for local reference but not
# tracked in git. Recovery: git history preserves the original
# locations via the Phase 4-6 JSX rewrite commits (7ec5c64, add234a,
# a1c3824, 53d1861).
_archive/
